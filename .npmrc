engine-strict=false
fund=false
audit=false
# force the PUBLIC npm registry — otherwise the machine's corporate proxy
# (artifactory.corp.xtb.com) gets baked into the lockfile and Vercel can't reach it
registry=https://registry.npmjs.org/
